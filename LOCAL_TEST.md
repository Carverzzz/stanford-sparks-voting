# 本地测试指南

## 🚀 快速开始

### 1. 启动本地开发服务器

```bash
cd /Users/carverz/Downloads/251203_Stanford_Spark_SCPA/stanford-sparks---two-truths-one-lie
npm install  # 如果还没安装依赖
npm run dev
```

### 2. 访问本地应用

打开浏览器访问：
- **后台控制**: http://localhost:5173/#/host
- **大屏幕**: http://localhost:5173/#/display
- **投票界面**: http://localhost:5173/

---

## 📋 CSV 格式要求

### 必需列名（必须包含）

| 列名 | 说明 | 示例 |
|------|------|------|
| `Name` | 参与者姓名 | 张三 |
| `Statement 1` | 第一个陈述（真实） | 我今年25岁 |
| `Statement 2` | 第二个陈述（真实） | 我喜欢吃苹果 |
| `Statement 3` | 第三个陈述（谎言） | 我会说5种语言 |

### 可选列

| 列名 | 说明 | 默认值 |
|------|------|--------|
| `Lie Index` | 谎言位置（1-3） | 3（第三个位置） |

---

## 📝 CSV 示例格式

```csv
Name,Statement 1,Statement 2,Statement 3,Lie Index
张三,我今年25岁,我喜欢吃苹果,我会说5种语言,3
李四,我去过10个国家,我每天跑步5公里,我是一名程序员,2
王五,我会弹钢琴,我养了一只猫,我从来没有坐过飞机,3
```

---

## ✅ 测试步骤

### 步骤 1: 准备 CSV 文件

1. 使用提供的 `test_data.csv` 文件，或
2. 创建自己的 CSV 文件，格式如下：

```csv
Name,Statement 1,Statement 2,Statement 3,Lie Index
测试1,我是程序员,我喜欢运动,我会说10种语言,3
测试2,我去过20个国家,我每天跑步,我从来没有坐过飞机,3
```

**重要**：
- 第一行必须是表头
- 使用英文逗号 `,` 分隔
- 文件编码必须是 **UTF-8**
- 保存为 `.csv` 文件

### 步骤 2: 导入 CSV

1. 打开后台界面: http://localhost:5173/#/host
2. 点击 **"Import Excel/CSV"** 按钮
3. 选择你的 CSV 文件
4. 应该看到成功提示："成功导入 X 位参与者！"

### 步骤 3: 验证导入结果

1. 检查参与者列表
2. 验证每个参与者的三个陈述是否正确显示
3. 验证谎言索引是否正确

### 步骤 4: 测试选项打乱

1. 选择一个参与者
2. 点击 **"Start Timer"** 创建投票轮次
3. 打开大屏幕和投票界面
4. 验证：
   - ✅ 三个选项的顺序是随机的
   - ✅ 每次创建新轮次，顺序可能不同
   - ✅ 谎言位置正确更新

### 步骤 5: 测试投票功能

1. 在投票界面选择一个选项
2. 验证：
   - ✅ 有动画效果
   - ✅ 大屏幕实时更新
   - ✅ 显示投票进度

---

## 🔍 支持的列名变体

系统会**不区分大小写**匹配列名，支持：

### Name 列
- ✅ `Name`
- ✅ `name`
- ✅ `群内微信名`
- ✅ `姓名`

### Statement 列
- ✅ `Statement 1` / `statement 1` / `真实陈述1` / `陈述1`
- ✅ `Statement 2` / `statement 2` / `真实陈述2` / `陈述2`
- ✅ `Statement 3` / `statement 3` / `谎言` / `一个假` / `陈述3`

### Lie Index 列（可选）
- ✅ `Lie Index` / `lie index` / `谎言索引` / `谎言位置`

---

## ⚠️ 常见问题

### 问题 1: 导入失败，提示"找不到列"

**解决**：
- 检查 CSV 文件第一行是否包含正确的表头
- 确保列名完全匹配（不区分大小写）
- 参考 `CSV_FORMAT.md` 查看支持的列名

### 问题 2: 中文显示乱码

**解决**：
- 确保 CSV 文件使用 **UTF-8** 编码保存
- 在 Excel 中：另存为 → 选择 "CSV UTF-8 (逗号分隔)(*.csv)"

### 问题 3: 选项没有打乱

**解决**：
- 检查浏览器控制台是否有错误
- 确保创建了新轮次（不是使用旧的轮次）
- 验证 `createRound` 函数中的打乱逻辑

---

## 📊 测试数据

已提供 `test_data.csv` 文件，包含 8 个测试用例：
- 桑倩倩（单个陈述，会重复）
- 乐以～Li Shaoxin（两个真实陈述）
- 景雯（两个真实陈述）
- 等等...

---

## 🎯 测试检查清单

- [ ] 本地服务器已启动
- [ ] CSV 文件格式正确
- [ ] 成功导入参与者
- [ ] 选项正确显示
- [ ] 选项顺序随机打乱
- [ ] 投票功能正常
- [ ] 实时更新正常

---

**现在可以开始本地测试了！** ✅

