# Google Sheets 格式优化建议

## ⚠️ 当前问题

"两个真"字段的自动拆分不够智能，对于复杂格式可能无法正确拆分。

## ✅ 推荐解决方案

### 方案一：在 Google Sheets 中分成两列（推荐）

**修改你的 Google Sheets 结构：**

| Timestamp | GENDER | 群内微信名 | MBTI | 择偶偏好 | **真实陈述1** | **真实陈述2** | **一个假** | 备注 |
|-----------|--------|-----------|------|---------|--------------|--------------|-----------|------|
| ... | F | fine | isfp | J | 喜欢运动 | 我会倒立 | 害怕孤独 | ... |

这样系统可以直接读取第6列和第7列，不需要拆分。

**需要修改代码以支持这种格式。**

---

### 方案二：使用统一的分隔符

在"两个真"字段中，使用统一的分隔符：

**推荐格式：**
- 使用分号：`喜欢运动；我会倒立`
- 使用换行：`喜欢运动\n我会倒立`
- 使用数字编号：`1. 喜欢运动 2. 我会倒立`

**系统支持的拆分格式：**
1. ✅ 数字编号：`1. xxx 2. xxx` 或 `1）xxx 2）xxx`
2. ✅ 分号：`xxx；xxx` 或 `xxx; xxx`
3. ✅ 换行符：多行输入
4. ✅ 句号：`xxx。xxx`
5. ✅ 逗号：`xxx，xxx`（如果其他分隔符不存在）

---

### 方案三：如果只有一个真

如果"两个真"字段只有一个陈述，系统会：
- 尝试进一步拆分
- 如果还是只有一个，会重复使用（标记为"重复"）
- 这样至少保证有三个选项

---

## 🔧 代码改进

我已经改进了拆分逻辑，现在支持：
- ✅ 更智能的数字编号识别
- ✅ 换行符分隔
- ✅ 句号分隔
- ✅ 按长度智能拆分（如果文本很长）
- ✅ 更好的错误处理

---

## 📝 建议

**最佳实践：**
1. 在 Google Sheets 中，将"两个真"分成两列（真实陈述1、真实陈述2）
2. 或者使用统一的分隔符（推荐分号 `；`）
3. 确保每个陈述都是完整的句子

**当前代码已支持多种格式，但如果格式不统一，可能无法完美拆分。**

