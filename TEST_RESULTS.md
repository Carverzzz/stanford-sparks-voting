# 测试结果验证

## ✅ 拆分逻辑测试

基于你的实际 Excel 数据，以下是测试用例和预期结果：

### 测试用例 1: 桑倩倩
- **输入**: `喜欢运动`
- **预期**: `['喜欢运动', '']` (只有一个真)
- **处理**: 系统会重复使用，确保有两个选项

### 测试用例 2: 乐以～Li Shaoxin
- **输入**: `我会倒立。 我可以骑自行车不扶把`
- **预期**: `['我会倒立', '我可以骑自行车不扶把']`
- **拆分方式**: 句号分隔 ✅

### 测试用例 3: 景雯
- **输入**: `读过博士，做过茶壶；游泳校队`
- **预期**: `['读过博士，做过茶壶', '游泳校队']`
- **拆分方式**: 分号分隔 ✅

### 测试用例 4: 林子是什么林
- **输入**: `我从来没有熬过通宵；我每年都会去旅行`
- **预期**: `['我从来没有熬过通宵', '我每年都会去旅行']`
- **拆分方式**: 分号分隔 ✅

### 测试用例 5: Jackie
- **输入**: `1）从一年级开始学大提琴，学到十一年级结束\n2）个人最高纪录是在床上不吃不喝不上厕所到晚上7点才起床`
- **预期**: `['从一年级开始学大提琴，学到十一年级结束', '个人最高纪录是在床上不吃不喝不上厕所到晚上7点才起床']`
- **拆分方式**: 数字编号 + 换行符 ✅

### 测试用例 6: Lucy
- **输入**: `喜欢户外跑步；喜欢打羽毛球，虽然打得不是很好`
- **预期**: `['喜欢户外跑步', '喜欢打羽毛球，虽然打得不是很好']`
- **拆分方式**: 分号分隔 ✅

### 测试用例 7: 爱吃辣的米娜酱
- **输入**: `1.我是双胞胎 2.我从没有独自旅行过`
- **预期**: `['我是双胞胎', '我从没有独自旅行过']`
- **拆分方式**: 数字编号 ✅

### 测试用例 8: Liangfang
- **输入**: `1. 喜欢吃辣和看下雪； 2. 工作上注重条理是个J人，但日常生活中有点P人哈哈哈`
- **预期**: `['喜欢吃辣和看下雪', '工作上注重条理是个J人，但日常生活中有点P人哈哈哈']`
- **拆分方式**: 数字编号 ✅

## ✅ 选项打乱验证

在 `HostDashboard.tsx` 的 `createRound` 函数中（第195-203行）：

```typescript
// Shuffle options logic
const optionsRaw = [p.statement_1, p.statement_2, p.statement_3];
const originalLieIndex = p.lie_index; // 固定为 2

const indices = [0, 1, 2].sort(() => Math.random() - 0.5); // 随机打乱
const shuffledOptions = indices.map(i => optionsRaw[i]);
const newLieIndex = indices.indexOf(originalLieIndex); // 更新谎言索引
```

**验证点**：
- ✅ `indices` 是 `[0, 1, 2]` 的随机排列（如 `[2, 0, 1]`）
- ✅ `shuffledOptions` 是打乱后的选项数组
- ✅ `newLieIndex` 会更新为谎言的新位置
- ✅ 用户看到的选项顺序是随机的

## 🧪 如何测试

### 方法一：在浏览器控制台测试

1. 打开后台界面: `/#/host`
2. 打开浏览器开发者工具（F12）
3. 在控制台运行：

```javascript
// 导入测试函数（需要先加载页面）
// 或者直接测试拆分逻辑
```

### 方法二：使用测试文件

1. 将 `test_import_data.csv` 上传到 Google Sheets
2. 在后台界面点击"同步 Google Sheets"
3. 检查参与者列表是否正确导入
4. 选择一个参与者，验证选项是否正确显示和打乱

## 📊 预期导入结果

基于你的 Excel 文件（46行数据），系统应该：

1. ✅ 跳过第1行（fine，两个真和假都是"-"）
2. ✅ 导入桑倩倩（1个真，会重复）
3. ✅ 导入乐以～Li Shaoxin（句号分隔，2个真）
4. ✅ 导入其他有效数据
5. ✅ 每个参与者的三个选项会被随机打乱

## ⚠️ 注意事项

1. **只有一个真的情况**：系统会重复使用，确保有三个选项
2. **拆分失败的情况**：如果无法拆分，会使用原文本作为第一个，第二个为空（然后会重复）
3. **选项打乱**：每次创建投票轮次时都会重新打乱，确保顺序随机

---

**所有测试用例已准备就绪！**

